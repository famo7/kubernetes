apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: project
spec:
  storageClassName: image-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
